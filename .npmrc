; react-day-picker@8 declares a React <=18 peer, but the app runs React 19 and
; works fine at runtime. Tolerate the peer mismatch so `npm ci` / `npm install`
; (locally and on Vercel) don't fail to resolve.
legacy-peer-deps=true
